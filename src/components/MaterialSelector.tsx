import { Tabs, Tab, Card, CardBody } from "@nextui-org/react";

export default function MaterialSelector() {
    return (
        <div className="flex w-full flex-col">
            <Tabs aria-label="Semester">
                
            </Tabs>
        </div>
    );
}